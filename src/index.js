import React from 'react';
import ReactDOM from 'react-dom';
import registerServiceWorker from './registerServiceWorker'; // For PWAs

import 'bulma/css/bulma.css';
import 'styles/app.css';

/**
 * Create a component called SeriesCard
 * The wrapper element should be a <div>
 * It should have an <img> with its SRC = http://static.tvmaze.com/uploads/images/medium_portrait/47/119795.jpg
 * It should have an <h1> with the series name
 * It should have a <p> with the series description
 * It should have a <p> with the series status RUNNING / ENDED
 *
 * You have max 10 minutes for doing this
 */

// ReactDOM.render(Your component here..., document.getElementById('root'));

// For PWAs
registerServiceWorker();
